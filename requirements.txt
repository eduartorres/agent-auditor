# Dependencias del Agente Auditor
# Instalación: pip install -r requirements.txt
#
# El proyecto usa exclusivamente librerías estables y ampliamente
# adoptadas en entornos de producción de analítica e IA.

# Embeddings y similitud semántica
sentence-transformers==2.7.0

# scikit-learn se usa puntualmente para cosine_similarity.
# Se prefiere sobre implementaciones manuales por su estabilidad numérica.
scikit-learn==1.4.2

# numpy como dependencia base de los módulos anteriores
numpy==1.26.4
